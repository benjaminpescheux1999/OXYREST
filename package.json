{
  "name": "oxyrest",
  "version": "1.0.0",
  "description": "API de pilotage OXYDRIVER (versionning, mapping utilitaire, proxy client).",
  "main": "dist/main.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "render-build": "npm ci && npm run build"
  },
  "dependencies": {
    "express": "^5.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
